# G-SAVE Project Instructions

本文档是 G-SAVE 仓库的根级项目约束。所有后续代理在规划、编码、测试和审查前都必须先阅读并遵守本文档。

## 1. 产品目标

G-SAVE 是 Windows 优先、基于 Git 的游戏存档管理工具。最高优先级是游戏运行期间的极低资源占用，其次才是功能丰富度、可观测性和开发便利性。

核心目标：

- Core 随用户登录自动启动，游戏按需热启动、热关闭。
- 游戏未运行是 Core 的主要常态，不是错误。
- 多个游戏可以同时运行，包括后台挂机游戏；每个游戏都必须正确监听和备份。
- 存档直接在原目录建立 Git 仓库，不复制影子工作区，也不使用外置 `GIT_DIR`。
- GUI 仅按需运行，关闭后不驻留。
- Git、存档解析和云同步等重型工作仅按需运行，不能常驻 Core。

## 2. 已锁定的架构边界

未经用户明确批准，不得改变以下决定：

- 不引入影子仓库或第二份常驻存档工作区。
- 不要求用户安装或分发完整 Git for Windows/PortableGit。
- libgit2 静态链接进 Core，但非仓库任务期间不得初始化或持有其运行时状态；Lua VM 和云同步状态同样只能按任务临时存在。
- 不分发独立 Git Worker EXE，不允许 Core 自启动为 Worker 模式。
- 非提交、推送等仓库任务期间，Core 只能有一个应用自身创建的线程；不得保留常驻 Worker 线程或线程池。任务期间可以按实际并发需求创建多个临时线程。
- 不增加 HTTP、RPC、Named Pipe 或其他 GUI/Core 业务通信服务。
- 不增加运行状态文件、钩子列表查询或通用状态 API。
- 不增加发布版常驻日志系统或周期性日志 I/O。
- 不增加常驻托盘、隐藏窗口、控制台或 GUI 进程。
- 不根据前台窗口判断哪个游戏需要备份。
- 不让文件事件回调执行复制、解析、Git 或网络操作。
- TOML 只保存配置，不保存运行状态、冲突命令或临时控制消息。

如果需求导致上述边界必须改变，先说明资源代价和必要性，获得用户确认后同步更新本文档。

## 3. 系统组成

系统分为四个部分：

1. `Core`：无窗口、事件驱动的常驻进程，负责进程事件、目录事件和任务调度。
2. `GUI`：支持包安装、配置、Core 启停、Git 历史、恢复和冲突处理。
3. `Game Package`：每游戏独立适配包，提供安装检测、进程/存档定位、忽略规则和存档元数据解析。
4. `Repository and Sync Engine`：静态链接进 Core/GUI，仅在任务期间由临时异步线程调用，并承载 Git、复制和同步适配器。

推荐的数据流：

```text
支持包 --GUI安装--> 解析具体路径并初始化仓库 --原子写入--> config.toml
                                                                  |
                                                                  v
游戏进程事件 --> Core --> 目录监听 --> 提交调度 --> 临时Git任务 --> 原地Git仓库
                                                                 \--> 同步适配器

GUI -------------------------------------------> Repository Engine --> 历史/恢复/冲突
```

## 4. TOML 配置契约

GUI 安装支持包时必须把 Core 注册回调所需的最终值写入主 TOML。Core 不依赖 GUI 运行，也不在运行时向 GUI 查询配置。

每个已启用游戏至少包含：

- 稳定的游戏 ID。
- 完整进程映像路径。
- 进程文件名。
- 一个或多个已经解析完成的存档仓库路径。
- 支持包或解析脚本路径。
- 提交策略。
- 同步后端和触发策略。

配置模型示例：

```toml
[[games]]
id = "game-id"
enabled = true
process_name = "game.exe"
process_path = "D:\\Games\\GameName\\game.exe"
parser = "packages/game-id/adapter.lua"

[[games.saves]]
path = "C:\\Users\\User\\AppData\\Roaming\\GameName\\Saves"
include_globs = ["*/Save*.dat"]
exclude_globs = [".git/**", "cache/**"]

[games.commit]
strategy = "hybrid"
quiet_seconds = 5
max_interval_seconds = 300
commit_on_exit = true

[games.sync]
backend = "git"
trigger = "on_exit"
```

约束：

- 不设置重复的 `save_root` 和 `repo_root`；存档目录本身就是仓库根目录。
- 多个互不相同的存档根目录使用多个 `[[games.saves]]`。
- GUI 必须把支持包安装结果中的 `include_globs` / `exclude_globs` 写入对应 `[[games.saves]]`；Core 在目录事件进入调度器前用这些轻量规则丢弃无关变化，缓冲区溢出事件除外。
- GUI 必须通过临时文件加原子替换更新 TOML。
- 普通配置变更通过 GUI 重启 Core 生效，不实现配置热重载。
- 密码和令牌不得明文写入 TOML；TOML 只保存系统凭据引用。

## 5. 游戏支持包

支持包至少包含：

```text
packages/<game-id>/
├── manifest.toml
└── adapter.lua
```

支持包通过 GUI 安装，不允许 Core 自行在线安装或更新支持包。

单个 Lua 适配脚本可以提供安装期和提交期入口：

```lua
function install(context)
    -- 检测游戏安装位置、进程名、存档目录和忽略规则。
    -- 返回 GUI 可以展示、确认并写入主 TOML 的结构化结果。
end

function parse(repository, changed_files)
    -- 返回槽位、角色名、游玩时间、存档地址等提交元数据。
end
```

GUI 流程分为两个独立步骤：

1. 导入支持包（ZIP）：只校验包结构和兼容版本，把整个包复制到主配置目录下的
   `packages/<game-id>/` 稳定位置。此步骤不运行 `install()`、不初始化 Git、
   不写主 TOML，也不要求游戏已安装。
2. 配置游戏支持：从已导入支持包（或内置通用包）开始，在受限 Lua 环境中执行
   `install()`，展示检测到的游戏路径、进程名和存档路径，由用户确认或修正；
   然后在每个存档目录原地初始化 Git 仓库，安装忽略规则和仓库级 Git 配置，
   创建初始基线提交，将最终进程和存档参数原子写入主 TOML，最后启动或重启 Core。

安装期 Lua VM 只在 GUI 操作期间临时存在。`install()` 自动检测成功时可一次返回多个仓库；检测不完整时 GUI 允许用户手动修正。确认安装后，GUI 必须先把整个支持包复制到主配置目录下的稳定位置，再把该副本中的解析脚本绝对路径写入 TOML，不能让运行配置长期引用源码树、下载缓存或用户临时导入目录。

如果安装时存档目录尚不存在，GUI 应提示用户先运行一次游戏并生成存档，然后重新检测；不得把首次安装发现逻辑转移到常驻 Core。

忽略规则优先写入 `.git/info/exclude`，避免在游戏目录根部增加额外控制文件。Git Remote 克隆到新设备后，由支持包安装流程重新生成本地忽略规则。

Lua 必须运行在受限环境中。它可以通过宿主提供的受控 API 检测目录、读取必要注册表项和返回忽略规则，但不得任意联网或启动外部程序。实际 Git 操作由 Repository Engine 完成。

没有正式支持包时，GUI 可以生成通用支持包。通用包由用户选择进程和存档目录，仍支持提交、恢复和同步，但元数据只能使用时间戳和通用文件信息。

## 6. Core 启动与运行流程

### 6.1 初始化

Core 启动后只执行必要工作：

1. 读取并校验 TOML。
2. 加载已启用游戏的轻量静态配置。
3. 校验进程映像路径、进程名、存档目录、`.git` 和解析脚本存在且可访问。
4. 建立进程匹配表：`进程名 + 完整映像路径 -> 游戏配置`。
5. 注册一个全局、事件驱动的进程启动/退出事件源。
6. 启动时枚举一次现有进程，以处理 Core 晚于游戏启动的情况。
7. 进入阻塞等待。

游戏当前没有运行属于正常状态。无法注册进程事件源或配置路径无效才属于初始化失败。

### 6.2 游戏启动

收到进程启动事件后：

1. 同时匹配进程文件名和规范化后的完整映像路径。
2. 为该进程创建轻量运行实例。
3. 对 TOML 已配置的存档目录建立异步目录监听。
4. 将所有目录句柄关联到同一个 IOCP。
5. 建立当前文件基线，避免遗漏监听挂载前的启动阶段写入。

Core 不在此阶段重新发现路径、初始化 Git 或生成忽略规则。

### 6.3 存档变化

目录事件处理只允许：

- 识别所属游戏和存档目录。
- 丢弃 `.git/**` 和支持包排除路径的事件。
- 设置 `dirty` 标记。
- 更新最后变化时间。
- 计算下一次稳定检查或周期提交截止时间。

禁止在回调中复制文件、加载 Lua、调用 libgit2、执行网络请求或等待子进程。

支持的提交策略：

- `quiet`：最后一次变化后安静指定时间再提交。
- `periodic`：持续写入时按最大脏状态间隔提交。
- `on_exit`：游戏退出时提交。
- `hybrid`：安静期、最大间隔和退出提交组合。

达到提交条件后，将任务加入队列；执行时才创建临时后台优先级线程调用 Repository Engine，任务结束后立即销毁该线程。多个游戏或仓库任务允许并发，不能因已有推送而阻塞其他存档提交。

### 6.4 游戏退出

相关进程退出后：

1. 对剩余脏状态执行最终提交。
2. 按配置尝试退出后同步。
3. 释放目录监听句柄和运行实例。
4. 回到空闲等待。

同一游戏或多个相关进程共享一个存档目录时必须使用引用计数；最后一个相关进程退出后才能释放监听和执行最终退出策略。

## 7. 多游戏并发模型

不得维护单一的“当前游戏”。后台挂机游戏也可能写入有效存档。

Core 只需维护两个轻量映射：

```text
PID -> 游戏配置、存档目录、运行实例
规范化存档路径 -> 目录监听器、引用计数、dirty和计时状态
```

约束：

- 所有存档目录共享一个 IOCP。
- 默认使用一个 Core 事件处理线程。
- 不为每个游戏创建线程或进程监听订阅。
- 不使用前台窗口、焦点或窗口可见性决定是否备份。
- 多个仓库同时需要提交时分别创建按需任务；不得用全局串行队列阻塞彼此。
- 多个游戏或仓库可以同时运行临时提交/推送任务；不能设置全局单任务瓶颈。所有临时线程都必须由实际任务触发，队列空闲时一个也不得存在。

## 8. 原地 Git 仓库

每个存档根目录直接执行 Git 初始化：

```text
游戏存档目录/
├── .git/
├── Save001.dat
├── Save002.dat
└── 其他游戏文件
```

硬性要求：

- 不创建影子工作区。
- 不创建外置 `GIT_DIR`。
- 当前存档只保留游戏目录中的一份。
- `.git/**` 永远不能触发存档 dirty。
- 优先监听支持包声明的精确存档文件或子目录，减少 `.git` 事件进入内核通知缓冲区。
- 如果必须递归监听整个仓库根目录，则过滤 `.git/**`；通知缓冲区溢出后重新扫描实际存档文件。
- 仓库必须关闭自动换行转换，保证二进制存档按原字节保存和恢复。
- 临时文件、缓存和非存档文件由支持包安装到 Git 忽略列表。

默认提交不复制完整影子快照：

1. 等待支持包配置的写入稳定条件。
2. 记录待提交文件状态。
3. 直接从真实存档生成 Git objects/tree。
4. 再次校验提交期间是否发生写入。
5. 有变化则放弃更新分支引用并重新等待。
6. 状态稳定后才创建或更新 commit。

未被引用的临时 Git objects 可以后续回收。只有明确无法通过安静期保证多文件一致性的游戏，支持包才能选择临时一致性快照策略；该策略不能成为全局默认。

恢复、checkout、主分支切换或冲突取舍会直接修改真实存档，因此必须确认目标游戏已经退出，并避免 Core 同时提交该仓库。

## 9. Repository Engine 与 libgit2

不分发完整 `git.exe`，也不提供独立 `gsave-git.exe`。Repository Engine 作为静态库链接进 Core 和未来 GUI；Core 只在提交或同步任务触发时调用。

Repository Engine 负责：

- 仓库初始化和初始提交。
- status、index、tree 和 commit。
- 差异、历史、checkout 和恢复。
- fetch、push、分歧检测、分支引用保存和按提交选择主时间线。
- 按需加载 Lua 解析器并生成提交元数据。
- 调用 Git、WebDAV、OneDrive 等同步适配器。

Core 中的 Repository Engine 必须：

- 非任务期间不得调用 `git_libgit2_init()`、创建 Lua VM 或保留同步后端状态。
- 任务到来时才按实际并发需求创建临时异步线程，并把线程 CPU 优先级设为低于正常；不得使用
  `THREAD_MODE_BACKGROUND_BEGIN`，因为它同时把磁盘 I/O 和内存优先级降到极低，会把几十 MiB
  的本地存档提交人为节流到数十秒甚至数分钟。不得预创建或缓存空闲线程。
- 任务完成后必须调用对应 shutdown、释放全部对象并销毁临时线程。
- 不创建常驻 Worker 线程、线程池、守护进程、辅助 EXE 或自启动 Worker 模式。
- 使用明确、受限的内部子命令，不实现任意 Git CLI 透传。

Windows 首版 libgit2 建议静态启用本地 Git 和 HTTPS，使用系统 WinHTTP；SSH 支持只有在明确需要后再加入，避免 libssh2 和额外密码学依赖。

## 10. GUI 职责

GUI 负责：

- 导入、升级、启用、禁用和生成支持包（导入只加入支持包库，不配置游戏路径）。
- 已导入支持包始终在「添加游戏支持」显示卡片，点击卡片即配置或修改游戏与存档路径；
  再次配置已有游戏时更新路径，保留启用状态和云端同步策略，不重复添加游戏。
- 检测并确认游戏路径、进程名和存档路径。
- 初始化原地 Git 仓库、忽略规则和初始提交。
- 原子更新 TOML。
- 启动、停止和重启 Core。
- 配置登录后自动启动。
- 配置 Git、WebDAV、OneDrive 和凭据引用。
- Git Remote 的玩家配置只要求服务地址和 Token：GUI 自动识别 GitHub、
  Gitee、GitLab 或 Gitea，读取当前账号，并按稳定游戏 ID 自动检测或创建
  私有仓库。仓库 URL、remote 名、分支和凭据引用不得作为普通玩家输入项。
- 云端设置自动作用于全部已配置游戏，不再提供“为哪个游戏备份”或逐游戏
  云端开关；是否保护某个游戏只由游戏支持页面的启用、暂停和移除控制。
- 浏览提交历史和适配器解析出的槽位、角色等元数据。
- 恢复历史、切换分支、拉取并按提交处理时间线分歧。
- 通过进程或启动项状态判断 Core 是否运行。

GUI 不通过 Core 执行 Git；GUI 与 Core 复用同一 Repository Engine 静态库代码。

多数游戏存档是二进制文件。冲突处理的最小粒度是完整提交/时间线，不是文件：界面不得提供逐文件合并、文本合并或把两端文件拼成一个新存档的能力。用户只能选择本地 head 或远端 head 继续作为主分支；未被选中的 head 必须先建立持久侧分支引用，作为完整分叉历史保留。默认不创建把双方内容汇合到同一棵树的 merge commit。

当用户选择本地时间线为主分支并需要发布到远端时，必须先把原远端 head 保存为持久保留分支，再更新远端主分支；不得在保留分支尚未确认可达时覆盖远端主引用。选择远端为主分支时，同样必须先为原本地 head 建立侧分支，再整体 checkout 远端提交。整个过程必须确认游戏退出并保持 Core 停止。

## 11. 同步适配层

需要支持：

- Git Remote。
- WebDAV。
- OneDrive 或其他后续云盘适配器。

推送触发策略：

- 每次提交后立即推送。
- 游戏退出后推送。
- 每隔指定时间统一推送。
- 仅手动推送。

本地 Git 提交必须独立于网络成功与否。网络失败或远端分歧不能破坏本地历史。

Git Remote 的 URL 不是本地提交策略的必填项。仅使用本地提交或手动推送时可以不配置任何远端 URL；选择每次提交、游戏退出或周期自动推送时才要求有效远端配置。

由于仓库直接位于存档目录，WebDAV/OneDrive 可以处理完整自包含仓库，但同步时必须：

- 等待 Git 操作结束并释放锁。
- 不上传 `.git/index.lock` 等临时锁文件。
- 保证对象内容先于 refs 可见。
- 从远端覆盖、恢复或解决冲突时确认游戏已退出并阻止 Core 同时提交。

WebDAV/OneDrive 的具体增量对象布局、原子更新和并发冲突协议仍需单独设计和测试，不得在未验证前简单递归覆盖一个正在使用的仓库。

## 12. 错误与可观测性策略

发布版 Core 正常运行时：

- 没有主窗口、托盘、隐藏窗口或控制台。
- 不写常驻运行日志。
- 不提供状态文件或诊断 API。
- 不保存钩子列表供 GUI 查询。

初始化阶段发现以下错误时，显示临时 Windows 错误对话框，释放已建立资源并以非零状态退出：

- TOML 无法解析或字段无效。
- 启用的支持包或解析脚本缺失、不兼容。
- 配置的游戏可执行文件不存在。
- 存档目录不存在、不可访问或不是有效 Git 仓库。
- 无法注册全局进程事件源。
- 已运行游戏无法建立所需目录监听。

游戏进程当前不存在不属于错误。

开发和测试期间可以添加临时诊断工具或测试输出，但不得作为发布版常驻行为保留。运行期错误按以下最小规则处理：

- 同一仓库的真实 commit 错误连续发生 3 次才弹窗并退出；前两次分别在 1 秒、3 秒后由现有事件等待截止时间重新触发。任意成功提交或确认无变化都会清零连续失败次数；提交期间存档仍在变化不计入失败次数。
- 单次 push 临时任务遇到网络错误时最多尝试 3 次，间隔为 1 秒、3 秒；重试发生在已经存在的临时任务线程内，不创建常驻重试线程、全局队列或后台定时器。
- 游戏仍运行时，3 次网络尝试均失败只保留本地历史并延迟一次提示到游戏退出；Core 不退出。下一次正常推送自然包含未推送的本地提交。
- 非网络 Git 错误、仓库损坏或时间线分歧属于需要用户处理的错误，弹窗并退出或交由 GUI 处理，不进行隐式内容合并。

可恢复的通知缓冲区溢出应重新核对存档状态，不得损坏仓库。

## 13. 性能硬约束

空闲 Core 必须满足以下结构性条件：

- 没有游戏运行时不打开存档目录句柄。
- 不初始化 Lua VM、libgit2 或云同步 SDK，不持有其堆状态。
- 不执行目录轮询、网络请求或周期性日志写入。
- 不为每个游戏创建线程。
- 非提交/推送任务期间只允许一个应用自身创建的线程；系统共享内存和系统内部辅助线程不计入应用自身指标。
- 应用私有常驻内存以 KB 为目标，必须用 Private Bytes 和 Private Working Set 实测；不得用共享映像页掩盖私有占用。
- 主要时间阻塞在系统事件等待上。

游戏运行期间：

- 文件回调只更新极小内存状态。
- 多目录共享 IOCP 和事件线程。
- 重型任务按实际仓库需求并发执行；同一仓库的互斥写操作由仓库锁串行化。
- 临时 Repository Engine 任务线程使用低于正常的 CPU 优先级，但保持正常 I/O 优先级；不得启用
  会造成极端 I/O 节流的 Windows 全后台模式。
- 临时任务线程结束后不得保留 libgit2、Lua 或同步运行时内存。

不要提前虚构 CPU、内存或唤醒次数指标。实现原型后必须分别基准测试：空闲、一个游戏、多个游戏、持续写入、提交、压缩、推送和冲突处理场景，再据实设定性能门槛。

## 14. 安全与并发约束

- GUI 修改仓库或真实存档前必须确认目标游戏已退出。
- Core 和 GUI 的 Repository Engine 不得同时对同一仓库执行互相冲突的写操作。
- 产品按正常单 GUI 使用设计，不增加 GUI 单实例、跨 GUI 互斥或为同时运行多个 GUI 的误操作提供防御状态机。
- 所有 Git 操作必须检查并正确处理仓库锁。
- 文件恢复必须采用可恢复的替换流程，不能留下半写入存档。
- Lua 脚本不得获得不必要的网络、进程启动或全盘写权限。
- 凭据不得进入 TOML、提交历史、错误对话框或测试制品。
- 支持包更新不得自动删除用户的 `.git` 历史。

## 15. 明确非目标

当前方案不包含：

- 面向局域网或互联网的 Core 服务端 API。
- 实时 GUI 状态推送。
- 通用系统钩子查看器。
- 常驻性能监控或遥测。
- 自动识别用户当前真正游玩的前台游戏。
- 在游戏运行时直接恢复、checkout 或切换真实存档主时间线。
- 完整兼容任意 Git CLI 命令、Git hooks、Git LFS 或任意第三方 credential helper。

## 16. 尚待验证或确认的技术决策

以下项目尚未锁定，不得擅自当作最终决定：

- Windows 全局进程启动/退出事件源的具体实现；必须通过常驻开销和可靠性实测选择。
- WebDAV/OneDrive 的增量布局、原子 refs 更新和多设备并发协议。
- 多账号、多槽位是共用仓库目录、分支还是由支持包返回多个独立仓库。
- 极端多文件阶段写入游戏是否需要包级临时一致性快照。

## 17. 后续代理工作规则

- 修改架构前先对照本文档，说明会影响哪些已锁定边界。
- 实现优先选择事件驱动、零轮询、零常驻依赖的方案。
- 新增任何常驻线程、进程、计时器、日志、IPC 或后台 SDK 前必须说明必要性和可测量成本。
- 不因为实现方便把 Git、Lua、网络或 GUI 依赖重新塞入 Core。
- 不因为同步方便创建第二份常驻存档目录。
- 所有模块边界都必须有接口测试，核心状态机和路径/事件合并逻辑必须有单元测试。
- Windows 句柄、异步回调、子进程和仓库锁必须采用明确的生命周期管理，并测试失败路径。
- 架构决定发生变化时必须同步更新本文档和相关 ADR，再修改实现。
